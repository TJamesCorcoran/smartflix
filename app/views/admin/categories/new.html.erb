<h1>New category</h1>

<%= error_messages_for :category %>

<%= form_for(:category, :url => categories_path) do |f| %>
  <%= render :partial => 'form' %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', categories_path %>
