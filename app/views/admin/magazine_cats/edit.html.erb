<h1>Editing magazine_cat</h1>

<%= error_messages_for :magazine_cats %>

<%= form_for(:magazine_cats, :url => magazine_cat_path(@magazinecat), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', magazine_cat_path(@magazinecat) %> |
<%= link_to 'Back', magazine_cat_path %>
